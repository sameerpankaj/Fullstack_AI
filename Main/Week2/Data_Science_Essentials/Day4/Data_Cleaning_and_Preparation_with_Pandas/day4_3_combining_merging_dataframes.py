'''
Combining and Merging DataFrames
--Concatenation
--Meging
--Joining


'''

import pandas as np


# #combining dataset
# combined_dataset = pd.concat([df1, df2], axis=0)
# combined_dataset = pd.concat([df1, df2], axis=1)

# #merging dataset
# merged_dataset = pd.merge(df1, df2, on='common_column')
# merged_dataset = pd.merge(df1, df2, how='left', on ='common_column')
# merged_dataset = pd.merge(df1, df2, how='inner', on ='common_column')

# #joining dataset
# joined_dataset = df1.join(df2, how='inner')